<p id="notice"><%= notice %></p>

<h1>Bookings</h1>

<table>
  <thead>
    <tr>
      <th>Destination</th>
      <th>Date and Time</th>
      <th>User</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @bookings.each do |booking| %>
      <tr>
        <td><%= booking.name %></td>
        <td><%= booking.user_id %></td>
        <td><%= link_to 'Show', booking %></td>
        <% if policy(booking).edit? %>
          <td><%= link_to 'Edit', edit_booking_path(booking) %></td>

        <% end %>
        <% if policy(booking).destroy? %>
          <td><%= link_to 'Destroy', booking, method: :delete, data: { confirm: 'Are you sure?' } %></td>
        <% end %>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New booking', new_booking_path %>


<%# LOUIS: %>
<div class="" >
  <h1>My bookings</h1>
  <ol>
  <% @bookings.each do |booking|%>
    <li> Date: <%=link_to booking.date, booking_path(booking)%>
    <br>
    <%= link_to 'Delete here', booking_path(booking),
      method: :delete,
      data: { confirm: "Are you sure?" } %>
    <p>____</p>
    </li>
  <% end %>
  </ol>

  <button type="button" class="button"><%=link_to 'See other available Trips', trips_path%></button>
</div>
